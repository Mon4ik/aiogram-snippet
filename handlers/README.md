## Every new activity add to `__init__.py`
Write in format: `from . import <filename>`
### For example:
New file (activity) with name **activity2.py**:
- Write in `__init__.py` line:
    ```python
    from . import activity2
    ```
- Done!